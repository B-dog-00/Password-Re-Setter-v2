/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/new-password-resetter',
  assetPrefix: '/new-password-resetter/',
  output: "export",
  reactStrictMode: true,
};

export default nextConfig;
